services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        NODE_VERSION: 22.20.0
        PNPM_VERSION: 10.17.1      
    image: brota/next-web:latest
    container_name: brota_landing
    environment:
      NODE_ENV: production
      PORT: 3000
    restart: unless-stopped
    networks:
      - proxy
    labels:
      - "traefik.enable=true"
      # -----------------------------
      # Router principal (HTTPS)
      # -----------------------------
      - "traefik.http.routers.fb.rule=Host(`fb.brota.tech`)"
      - "traefik.http.routers.fb.entrypoints=websecure"
      - "traefik.http.routers.fb.tls=true"
      - "traefik.http.routers.fb.tls.certresolver=myresolver"
      # -----------------------------
      # Servicio backend
      # -----------------------------
      - "traefik.http.routers.fb.service=fb-svc"
      - "traefik.http.services.fb-svc.loadbalancer.server.port=8080"
      # -----------------------------
      # Redirect HTTP -> HTTPS (usa el middleware definido en el contenedor traefik)
      # -----------------------------
      - "traefik.http.routers.fb-redirect.rule=Host(`fb.brota.tech`)"
      - "traefik.http.routers.fb-redirect.entrypoints=web"
      - "traefik.http.routers.fb-redirect.middlewares=redirect-to-https@docker"
      # -----------------------------
      # Indica la network que Traefik debe usar para reachability (opcional/útil)
      # -----------------------------
      - "traefik.docker.network=proxy"
networks:
  proxy:
    external: true
